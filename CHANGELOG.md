# 变更记录

## 未发布

- 修复重复 Compaction 丢失上一轮保留边界：原来按最近一条 compaction 后的条目自行切分，已有大摘要且新增内容未满 `keepRecentTokens` 时会错误地让 Jev 返回“没有可压缩内容”；现在直接使用 Pi preparation 的 `messagesToSummarize`、split-turn 前缀和 `firstKeptEntryId`。用 LYStar Code 对「原系统园区及客户合同缴费数据迁移方案」真实会话验证：`118700 → 25059 Token`、`fromHook=true`、1 个请求、615ms。
- 压缩无事可做时不再报英文回退：整段历史都在 Pi 保留窗口内（`keepRecentTokens` 默认 20,000）时会弹 `Jev 压缩回退：nothing to compact before kept window`，现在改为中文说明「整段历史都在保留窗口内，没有可压缩的内容」并按 info 提示；用户取消不再提示；其余回退原因（分支状态异常、无候选、Jev 请求失败）一并中文化。实测刚压缩过的会话（压缩后新增 33 条、36 KB）不再出现英文告警。
- 摘要预算改为「窗口份额 + 绝对上限」：`clamp(0.4 × 上下文窗口, 16000, 120000)`，`tokensBefore` 兜底路径同规则。修复 1M 窗口模型产生 189,844 Token 常驻摘要的问题（同一会话在 272,000 与 1,000,000 窗口下的摘要体积差 10 倍）。
- 摘要降级按可恢复性排序：受保护写操作入参在紧凑档位从原文改为 1,000 字符开头（实测 92 次写调用的入参共 191KB、占摘要 70%，截断后省 61%），条目、文件路径与动作保留；最后一档不再整段丢弃最老消息，用户消息、工具失败和写操作目标仍保留。实测同一会话从 `bounded` 18,526 Token（用户消息几乎全失）变为 `skeleton` 106,040 Token（减少 90%，用户消息与写入记录完整）。
- 修复压缩预算低估：状态原来按原始文本计量，而请求体里是 JSON 转义后的形态（换行、引号多占字符并切断字母片段），实际约为估算的 1.4 倍。现在状态与问题都按请求体形态计量，问题正文与提问共用同一个构造，每条问题把 JSON 分隔符计入。新增测试直接核对实际捕获的请求体不超预算。实测真实会话两个请求体分别为 29,588 / 24,556 Token（预算 30,000），我们的估算比 API 计费输入高 1.3 倍，偏保守。
- 新增摘要有界档：逐条展开也压不进摘要预算时，只保留最近 200 条旧消息的细节，更早的只留计数。实测 700+ 条旧消息、429 次工具调用的会话从「摘要超预算回退到主模型摘要」变为 2 个请求、1.78s、旧内容减少 97%。
- 修复大会话压缩直接回退到 Pi 原生摘要的问题：状态挤不进 `maxStateTokens` 时新增 `minimal_bounded` 降级，按调用数封顶（200 条）逐条渲染，并只对状态里渲染出来的调用提问，其余按保留处理。实测 697 条旧消息、363 次工具调用的会话从「状态超限 30ms 回退」变为 1 个请求、1.94s、旧内容减少 88%。
- Jev 请求预算收敛到 `typesafe-core.mjs` 单一事实源（`TYPESAFE_REQUEST_CHARS` / `TYPESAFE_REQUEST_TOKENS` / `TYPESAFE_REQUESTS_PER_ROUND`）；Compaction 的 30000/20000 Token 预算改为从中推导，数值不变。
- anti-slop 的请求上限统一为“每轮请求数”：原来的批次上限与候选上限合并为同一个量，`MAX_HUNKS_PER_FILE` 保留为单独的覆盖范围上限。
- anti-slop 的跨轮次状态显式化：`createAuditState()` 持有路由结果、候选进度与项目事实，候选按 `awaiting_evidence → judged` 推进；证据超量丢弃时记录偏移，修正已登记取证据起点错位的隐患。
- 优化 anti-slop 复核阶段：多个候选合并为一次请求并按请求体预算分批，12 个候选从 12 次请求降到 1 次；候选顺序与失败语义保持不变。
- 规则新增可选 `appliesTo` 字段声明适用的文件扩展名：非页面文件不再询问九条页面规则，60 个片段的初筛问题数从 780 降到 240、体积从 206KB 降到 94KB；页面文件行为不变。
- 修复 anti-slop 批量初筛超出 Jev Token 预算导致整轮审计失败：按请求体预算（64KB）分批，并把 `MAX_ROUTE_CALLS` 恢复为批次数上限。实测 60 个片段从一次失败变为 4 批、0 错误。
- 请求层在非 2xx 响应时保留响应体原因（截断 200 字符），不再只报状态码。
- 优化 anti-slop 语义初筛：规则范围与违规定义只在 state 中声明一次，问题只引用规则编号，12 个片段的请求体积从 51177 降到 37306 字符；初筛门槛按会话日志标定从 0.55 提到 0.75，可去掉约 80% 的待取证噪音。
- 修复 Compaction 性能回退：状态只拟合一次并在所有请求间共享，只在问题超出请求预算时才分批；增加防摘要嵌套和共享状态测试。
- Compaction 状态拟合在超出 Token 预算时提前停止，不再构造并全量扫描必然超预算的状态文本；合成 300/600 调用的准备耗时从 337ms/624ms 降到 33ms/42ms，状态与决策输出不变。
- 优化 Guard：在当前用户请求和分支内复用相同动作判断，工具失败后清空判断缓存。
- 优化 Skill Planner：显式 Skill 和无候选 Skill 走本地路径，显式与非显式请求都按请求指纹复用计划（同一显式请求 10 次从 10 次询问降到 2 次），压缩注入主模型的指导块（368—478 字符降到 141—165 字符）。
- 将 `lystar-jev-guard`、`lystar-jev-compaction`、`lystar-jev-skill-planner` 和 `lystar-jev-anti-slop` 拆成独立入口。
- 将会话压缩从 Guard 移出，默认包只加载 Guard、Compaction 和 Skill Planner。
- 将 anti-ai-slop 改为选装的变更复核 Extension，不再由 Guard 自动注册。
- 增加四个 Extension 的组合加载边界测试。

## 0.1.0 - 2026-09-20

- 首次整理 `lystar-jev-guard` 和 `lystar-jev-skill-planner` 为 Pi Package。
- 增加共享 TypeSafe API 客户端、配置和凭据读取模块。
- 随包分发 anti-ai-slop 默认规则。
- 增加测试、类型检查、包检查和 GitHub Actions 工作流。
